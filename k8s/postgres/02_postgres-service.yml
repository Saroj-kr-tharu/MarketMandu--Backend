apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: marketmandu-ns
spec:
  clusterIP: None
  selector:
    app: postgres-dep
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres