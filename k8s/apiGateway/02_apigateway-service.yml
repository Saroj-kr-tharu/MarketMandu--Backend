apiVersion: v1
kind: Service
metadata:
  name: apigateway-svc
  namespace: marketmandu-ns
spec:
  selector:
    app: apigateway-dep
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000